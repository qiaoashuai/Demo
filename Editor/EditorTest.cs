using UnityEngine;
using UnityEngine.UI;

public class EditorTest : MonoBehaviour
{
    public Image testImage;
    // Start is called before the first frame update
    void Start()
    {
        Debug.LogError("this  is Test EditorTest");   
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
